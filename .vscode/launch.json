{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Next.js: debug",
            "type": "node-terminal",
            "request": "launch",
            "command": "npm run dev",
            //"command": "npm install; npm run build; npm run start",
            "cwd": "${workspaceFolder}"
        }
    ]
}
